import { CytonSettings, DataStream4Ch, DataStream8Ch, GanglionSettings } from "../Interfaces";

export class MIDIManager {
    public convertInput(data:DataStream8Ch | DataStream4Ch, settings:CytonSettings | GanglionSettings) {
        
    }
}